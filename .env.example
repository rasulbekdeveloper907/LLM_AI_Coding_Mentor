# Copy this file to .env and fill in your own values.
# .env itself is in .gitignore and must never be committed.
#
# A token that has been typed into a notebook cell is a token that is already
# leaked - a screenshot or a commit is all it takes.

# Only needed for private or gated Hugging Face datasets.
# TinyStories is public, so you can leave this empty for now.
HF_TOKEN=
